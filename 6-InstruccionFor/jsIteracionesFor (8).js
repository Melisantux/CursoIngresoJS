function mostrar()
{
    //como calcular y contar numeros primos

if (numero%i== 0) {
    contadordiv++;
    console.log(i)
}
if (contadordiv != 2) {
    //no es primo
    // otro metodo

for (var i=2; i < numero; i++) {
    // agregar un if para cuando el numero es 2.
    if (num%i==0) {
        alert ("no es primo.");
        break;
    } else {
        alert("Es primo");
    }
}
}

//Ejercicio de clase: pedir un número (1000), calcular y mostrar la cantidad de números primos ( y cuales son) entre 1 y el número ingresado.



}//FIN DE LA FUNCIÓN