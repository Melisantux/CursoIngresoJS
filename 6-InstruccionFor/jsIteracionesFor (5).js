function mostrar()
{
    var numero;
    for(var contador=0; ; contador++) {
        numero= prompt("Ingrese un número");
        numero= parseInt(numero);
        if (numero==9) {
            break;
        }
    }
}//FIN DE LA FUNCIÓN